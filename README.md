# flask-student-api

Python RESTful API built with Flask that allows users to perform CRUD operations (Create, Read, Update, Delete) on student data. The API stores and manipulates student information such as name, degree, and degree using student IDs. It utilizes an SQLite database, along with with SQLAlchemy ORM for simple and efficient data management.
